# Пароль для доступа ко всем разделам кроме трактографии (главная `/` без пароля).
# Если не задан, в dev используется пароль по умолчанию `demo` (см. src/features/app-auth/lib/access-password.ts).
VITE_APP_ACCESS_PASSWORD=
