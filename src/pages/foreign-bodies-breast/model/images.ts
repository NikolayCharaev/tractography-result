export const foreignBodiesBreastImages = Array.from(
  { length: 10 },
  (_, index) => `foreign-bodies/breast/${index + 1}.jpg`,
)


export const foreignBodiesHipImages = Array.from(
  { length: 10 },
  (_, index) => `foreign-bodies/breast/${index + 1}.jpg`,
)
